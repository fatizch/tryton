<?xml version="1.0"?>
<data>
    <xpath expr="/form/notebook" position="inside">
        <page string="Invoicing" id="invoicing">
            <label name="payment_term"/>
            <field name="payment_term"/>
            <field name="payment_terms" colspan="4"/>
            <label name="invoice_frequency"/>
            <field name="invoice_frequency"/>
            <field name="invoice_frequencies" colspan="4"/>
        </page>
        <page string="Premium" id="premium">
            <field name="premiums"/>
        </page>
    </xpath>
</data>
