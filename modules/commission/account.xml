<?xml version="1.0" encoding="utf-8"?>
<tryton>
  <data>
    <!-- ################## -->
    <!-- # Move Breakdown # -->
    <!-- ################## -->
    <record model="ir.ui.view" id="breakdown_view_list">
      <field name="model">account.move.breakdown</field>
      <field name="type">tree</field>
      <field name="name">breakdown_list</field>
    </record>
    <!-- ############################################################ -->
    <!-- Move -->
    <!-- ############################################################ -->
    <record model="ir.ui.view" id="move_form">
      <field name="model">account.move</field>
      <field name="type">form</field>
      <field name="inherit" ref="billing_individual.move_form"/>
      <field name="name">move_form</field>
    </record>
    <record model="ir.ui.view" id="move_tree">
      <field name="model">account.move</field>
      <field name="type">tree</field>
      <field name="inherit" ref="billing_individual.move_tree"/>
      <field name="name">move_tree</field>
    </record>
  </data>
</tryton>
