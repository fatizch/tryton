<?xml version="1.0"?>
<form string="Preview Contracts Payments">
    <group id="one_contract" string="" states="{'invisible': Len(Eval('contract_previews', [])) != Len([0])}" yexpand="1" yfill="1">
        <field name="contract_previews" mode="form" expand_toolbar="0"/>
    </group>
    <group id="multiple_contract" string="" states="{'invisible': Len(Eval('contract_previews', [])) == Len([0])}" yexpand="1" yfill="1">
        <field name="contract_previews" mode="tree,form"/>
    </group>
</form>
