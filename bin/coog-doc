# vim: set ft=sh:
# coog documentation

coog_doc_generate() { # Generates coog documentation
    (
        cd .. && \
        docker-compose -f doc/docker-compose.yml up  
        docker-compose -f doc/docker-compose.yml down -v --rmi all
    )
}

