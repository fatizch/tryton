#!/bin/sh

docker run \
        -d \
        --name coog \
        --link postgres:postgres \
        -e "COOG_DB_USER=postgres" \
        -e "COOG_DB_PASSWORD=postgres" \
        -p 8000:8000 \
        coog/coog \
        app -d coog
