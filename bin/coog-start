# vim: set ft=sh:
# dev start shortcut (server and client)

coog_start_main() { # start all, [args] passed to server and client, eg. coog start -- -v
        coog_server_start $*
        coog_client_start $*
}
